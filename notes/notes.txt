
U ????

When string is NULL

When * is passed as the flag, if then number is negative the sign is ignored for
some reason

When pointer is NULL != When a pointer is 0

Precision with pointers

When just using %5%

c is NULL
